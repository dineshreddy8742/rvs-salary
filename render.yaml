services:
  - type: web
    name: rvs-salary
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 2 --worker-class gthread
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: SUPABASE_DB_URL
        value: "postgresql://postgres.nfqtqgqbuaotkljrsmpb:Reddy%407989604033@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require"
